{
  "name": "auto-calling-crm",
  "version": "0.1.0",
  "private": true,
  "description": "Auto Calling CRM MVP with simulated outbound/inbound call flows, IVR, live transfer, callbacks, and reports.",
  "scripts": {
    "start": "node backend/server.js",
    "check": "node --check backend/server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}
